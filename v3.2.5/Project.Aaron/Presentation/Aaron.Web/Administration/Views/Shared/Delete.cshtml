@model Aaron.Core.Web.Mvc.DeleteConfirmationModel
@using (Html.BeginForm(Model.ActionName, Model.ControllerName, new { id = Model.Id }))
{
    <div>
        <p>
            Bạn chắc chắn muốn xóa?
        </p>
        <div class="form-actions">
            <span class="t-button" onclick="closeModalWindow();">Không</span>
            <input type="submit" class="t-button" value="Xóa" />
        </div>
    </div>
}